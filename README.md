# 🧠 TP Redis sur GitHub Codespaces

## 🎯 Objectif du TP
Ce TP a pour but d’installer, configurer et sécuriser **Redis** dans un environnement **GitHub Codespaces**.  
Redis est une base de données clé-valeur en mémoire, rapide et légère, souvent utilisée pour la mise en cache, la gestion de sessions et le traitement en temps réel.

---

## ⚙️ Installation de Redis

Redis est automatiquement installé via le fichier `.devcontainer/Dockerfile` au moment où vous ouvrez le Codespace.

Vérifiez l’installation :
```bash
redis-server --version

Lancez Redis :

redis-server

